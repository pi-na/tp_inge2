# Ejemplo de override para desarrollo local
# Copia este archivo a docker-compose.override.yml y ajusta según necesites

version: '3.8'

services:
  frontend:
    environment:
      # Cambiar si accedes desde otro dispositivo (ej: celular en la misma red)
      - VITE_API_URL=http://TU_IP_LOCAL:8000
      # O si usas ngrok:
      # - VITE_API_URL=https://tu-backend-ngrok.ngrok-free.app

